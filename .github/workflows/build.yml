name: CI / Build

on:
  push:
    branches: [master, main]
    tags: ["v*"]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Syntax check
        shell: bash
        run: python -m py_compile slink/*.py
      - name: Quick import test
        run: |
          python -c "from slink.cli import main; print('cli OK')"
          python -c "from slink.gui import main; print('gui OK')"

  build-windows:
    runs-on: windows-latest
    needs: test
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt nuitka
      - name: Build sli CLI
        run: python -m nuitka --standalone --remove-output --output-dir=dist --assume-yes-for-downloads build_sli.py
      - name: Build sli-ui GUI
        run: python -m nuitka --standalone --remove-output --output-dir=dist --assume-yes-for-downloads build_sli_ui.py
      - name: Upload sli artifact
        uses: actions/upload-artifact@v4
        with:
          name: sli-windows-x64
          path: dist/build_sli.dist/
      - name: Upload sli-ui artifact
        uses: actions/upload-artifact@v4
        with:
          name: sli-ui-windows-x64
          path: dist/build_sli_ui.dist/

  build-linux:
    runs-on: ubuntu-latest
    needs: test
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-tk
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt nuitka
      - name: Build sli CLI
        run: python -m nuitka --standalone --remove-output --output-dir=dist --assume-yes-for-downloads build_sli.py
      - name: Build sli-ui GUI
        run: python -m nuitka --standalone --remove-output --output-dir=dist --assume-yes-for-downloads build_sli_ui.py
      - name: Upload sli artifact
        uses: actions/upload-artifact@v4
        with:
          name: sli-linux-x64
          path: dist/build_sli.dist/
      - name: Upload sli-ui artifact
        uses: actions/upload-artifact@v4
        with:
          name: sli-ui-linux-x64
          path: dist/build_sli_ui.dist/

  build-macos:
    runs-on: macos-latest
    needs: test
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt nuitka
      - name: Build sli CLI
        run: python -m nuitka --standalone --remove-output --output-dir=dist --assume-yes-for-downloads build_sli.py
      - name: Build sli-ui GUI
        run: python -m nuitka --standalone --remove-output --output-dir=dist --assume-yes-for-downloads build_sli_ui.py
      - name: Upload sli artifact
        uses: actions/upload-artifact@v4
        with:
          name: sli-macos-x64
          path: dist/build_sli.dist/
      - name: Upload sli-ui artifact
        uses: actions/upload-artifact@v4
        with:
          name: sli-ui-macos-x64
          path: dist/build_sli_ui.dist/

  release:
    runs-on: ubuntu-latest
    needs: [build-windows, build-linux, build-macos]
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Package
        run: |
          cd artifacts
          zip -r sli-windows-x64.zip sli-windows-x64/
          zip -r sli-ui-windows-x64.zip sli-ui-windows-x64/
          tar czf sli-linux-x64.tar.gz sli-linux-x64/
          tar czf sli-ui-linux-x64.tar.gz sli-ui-linux-x64/
          tar czf sli-macos-x64.tar.gz sli-macos-x64/
          tar czf sli-ui-macos-x64.tar.gz sli-ui-macos-x64/
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/*.{zip,tar.gz}
          generate_release_notes: true
